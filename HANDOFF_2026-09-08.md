# 개편 통합 정리 · GPT 인계 (2026-09-08)

개편 작업 전체를 한 장으로 정리한 문서입니다. **이 문서 하나만 읽어도 지금 어디까지
왔고 다음에 무엇을 해야 하는지 알 수 있게** 썼습니다. 세부는 각 문서로 넘깁니다.

- 캠페인 구조와 게시판 규칙 → `HANDOFF_CAMPAIGN_2026-09-02.md`
- 상담소(`pb.html`) 다루는 법 → `HANDOFF_PB_2026-09-08.md`
- 어느 파일이 원본이고 어느 것이 생성물인지 → `CAMPAIGNERS_CODE_SPLIT.md`
- 작업 규칙 · 결정 기록 → `WORKING_RULES.md` · `README.md`(맨 위가 최신)

---

## 0. 한 줄 요약

**화면 쪽 작업은 사실상 끝났습니다. 캠페이너스 이식을 진행하세요.**

남은 것은 (1) 사람이 채워야 하는 활동 화면 셋의 내용, (2) `dok.action.or.kr` HTTPS
인증서 — 둘 다 **이식을 막지 않습니다.** 아래 0-1 을 보세요.

---

### 0-1. 이식은 갈라서 하면 됩니다

`확인 필요` 딱지가 남은 곳은 **활동 화면 셋뿐**입니다. 나머지는 지금 붙여도 됩니다.

| 지금 붙여도 되는 것 | 내용을 받은 뒤에 |
|---|---|
| `/69` 홈 · `/77` 함께하기 · `/76` 밑빠진 독상 | `/49` 지자체 감시 |
| 공통 상단 · 공통 하단 (반복 섹션) | `/27` 예산감시 |
| 후원 안내 | `/51` 시민참여 |

공통 상단·하단은 **반복 섹션의 코드 위젯 한 곳**이라, 나중에 고쳐도 아홉 화면을
다시 붙이지 않습니다. 활동 셋도 나머지와 독립적입니다.

⚠️ **지금 캠페이너스에 붙어 있는 상단은 2026-09-07 이전 옛 코드입니다** — 화면에
로그인·회원가입이 남아 있으면 그것입니다. 상단 반복 섹션을 다시 붙이면 사라집니다
(로그인은 도너스 마이페이지로 옮겨 후원 목차의 '회원정보 수정' 이 됐습니다).

---

## 1. GPT 가 기다리던 답 — 세 캠페인 사이트 주소와 접속 상태

2026-09-02 인계 때 "세 사이트의 최종 주소와 HTTPS 정상 접속 여부가 확인돼야 상단
캠페인 메뉴를 외부 주소로 바꾼다"고 적혀 있었습니다. **오늘 재고, 반영까지 끝냈습니다.**

| 캠페인 | 최종 주소 | 2026-09-08 확인 | 서비스 |
|---|---|---|---|
| 2026 하반기 밑빠진 독상 | `https://dok.action.or.kr` | ⚠️ **HTTPS 인증서 아직** (아래 1-1) | GitHub Pages |
| 시민참여 상담소 | `https://pb.action.or.kr` | ✅ 200 · `http`→`https` 301 · 인증서 12-07 까지 | GitHub Pages |
| 10만원 예산편성 투표 | `https://vote.action.or.kr` | ✅ 200 (홈 카드 사진 `og-budget-vote-v1.png` 도 200) | — |

- `vote` 는 2026-09-03 에 통째로 404 였는데 **되살아났습니다.**
- **`*.pages.dev` 주소는 이제 어디에도 걸지 않습니다.** 상담소는 Cloudflare Pages 를
  안 쓰고, 독상도 깃허브 페이지로 옮겼습니다(Cloudflare 프로젝트는 롤백용으로만 남김).

**반영한 곳** — 커밋 `a2dc965`(독상) · `54bf096`(상담소).
`index.html` 슬라이드와 캠페인 목록, `issue.html` 캠페인 카드,
`tools/parts/header.html` 머리 차림표 두 곳(넓은 화면·좁은 화면), 문서 넷.
생성물(`campaign-together-layout.html`, `dist/`)도 다시 구웠고
`python tools/check.py --net` 은 걸리는 것 없음.

### 1-1. `dok.action.or.kr` HTTPS — 진행하면서 지켜봅니다 (2026-09-08 사용자 결정)

DNS 는 정상입니다(`CNAME → bada17.github.io`, `http://` 는 200). 그런데 **인증서가
`CN=*.github.io` 라 이름이 안 맞습니다.** `gh api repos/bada17/dokseong/pages` 응답에
`https_certificate` 키가 아예 없습니다 — 발급 요청이 걸려 있지 않다는 뜻입니다.

**이식을 막지 않습니다.** 붙여 넣는 코드에는 주소(`https://dok.action.or.kr`)만
들어갑니다. 인증서가 나오면 **이미 붙여 놓은 링크가 저절로 살아납니다** — 코드를
다시 손댈 일이 없습니다. DNS 도 도메인 등록도 2026-09-08 에 했으니, 아직 첫 재시도
차례가 안 왔을 수 있습니다. **하루 두고 봅니다.**

⚠️ **`CNAME` 파일을 지웠다 되돌리는 방법은 이 저장소에서 안 통합니다.** 상담소
(`pb-action-site`)는 브랜치 방식이라 파일이 도메인을 정하지만, 독상은 **Actions
워크플로 방식**이라 도메인이 **저장소 설정에 따로 저장**됩니다. 2026-09-08 에
파일을 빼고 배포까지 성공시켰는데 `cname` 이 그대로였습니다(확인함).

저절로 안 나오면 **설정 화면**에서 뺐다 넣어야 합니다 — 재시도 시계가 버려집니다.

```
1) https://github.com/bada17/dokseong/settings/pages
2) Custom domain 을 비우고 Save → 30초 뒤 dok.action.or.kr 를 다시 넣고 Save
3) DNS check successful 이 뜨고 인증서가 나오면
   gh api -X PUT repos/bada17/dokseong/pages -F https_enforced=true
```

⚠️ **공개(대표 홈 전환) 전에는 반드시 끝나 있어야 합니다.** 안 그러면 방문자가
밑빠진 독상을 누를 때마다 브라우저 경고를 봅니다. **이식과 검토에는 지장 없습니다.**

⚠️ **`tools/check.py --net` 은 이 문제를 못 잡습니다.** 인증서 검증을 꺼 두고
(`ssl.CERT_NONE`) 부르기 때문입니다. HTTPS 는 `openssl s_client -connect <주소>:443
-servername <주소>` 로 `subject` 를 직접 보세요.

---

## 2. 지금 상태 한눈에

| | 값 |
|---|---|
| 저장소 | `bada17/action-home-renewal` (**공개**), 기본 브랜치 `main` |
| 로컬 | `…\밑빠진독상 페이지\action-home-renewal` (GPT 와 **같은 폴더**) |
| 최신 커밋 | `f7c1724` 까지 원격에 올라가 있습니다(2026-09-08). 그 뒤 헤더 크기 수정 하나 |
| 검토 사이트 | <https://bada17.github.io/action-home-preview/> — `main` 에 push 하면 자동 |
| 공개 홈 | 아직 아닙니다. 캠페이너스 숨김 페이지 `/69`, 대표 홈 전환 안 켬 |

**캠페이너스 페이지 번호** — `/69` 홈(숨김) · `/76` 밑빠진 독상 · `/77` 함께하기 ·
`/81` 진행 중 캠페인 게시판 · `/82` 종료(상단 메뉴에는 안 냄).
`/78·/79·/80` 은 **만들지 않습니다** — 개별 캠페인은 위 외부 주소로 바로 갑니다.

---

## 3. 남은 일 — 누가 무엇을

### 3-1. 사용자만 할 수 있는 것

1. **캠페이너스에 붙이기** — 코드 위젯은 손으로 붙이는 구조라 화면마다 사람이 합니다
   (섹션 반복도 사용자 몫). 순서는 `CAMPAIGNERS_CODE_SPLIT.md`, 무엇부터인지는 0-1.
2. **활동 화면 내용 채우기** — `확인 필요` 딱지가 붙은 자리입니다. 아래 4번.

~~3. `dok.action.or.kr` HTTPS~~ → **2026-09-09 에 끝났습니다.** 저장소 설정 화면에서
Custom domain 을 비웠다 다시 넣으니 그 자리에서 인증서가 나왔습니다. `Enforce HTTPS`
도 켰습니다. (`CNAME` 파일을 지웠다 되돌리는 방법은 이 저장소에서 안 통합니다.)

~~4. `git push` — Claude 는 막혀 있습니다~~ → **아닙니다. Claude 가 직접 푸시합니다**
(2026-09-09). 배포도 마찬가지입니다 — 그날 상담소를 굽고 배포본 저장소까지 넘겼습니다.

### 3-2. GPT

1. **`assets/donus/` 와 `tools/make-donus-logo-assets.py`** — 폴더에 커밋 없이 놓여
   있는데 저장소 어디에서도 부르지 않습니다. 후원 안내(`donate-guide.html`)에 쓸
   것이라면 그 화면과 **한 커밋으로** 올려 주세요. 안 쓸 것이면 지워도 됩니다.
   (남의 미커밋 파일이라 Claude 는 그대로 두었습니다.)

### 3-3. Claude (내용을 받으면 바로)

- `확인 필요` 딱지 떼기, 활동별 '자료실' 링크가 다섯 다 전체 자료실로 가는 것 고치기.

---

## 4. `확인 필요` 딱지 — 실제로 화면에 나가는 것은 몇 개 안 됩니다

`python tools/check.py` 는 **소스 기준 70곳**이라고 셉니다. 그런데 `build-static.py`
가 주석을 걷어내므로 **실제 화면에 나가는 것은 훨씬 적습니다.**

굽고 난 `dist/` 기준으로 화면에 딱지가 뜨는 곳은 **활동 화면 셋뿐**입니다 —
`/27` 예산감시(5) · `/49` 지자체 감시(5) · `/51` 시민참여(6). 일정·기간·결과 카드입니다.
`/76` 밑빠진 독상에 하나 걸리는 것은 설명 문구라 **화면에 뜨지 않습니다**.
홈 · 함께하기 · 후원 안내는 **깨끗합니다.**

- 고칠 곳은 화면 파일이 아니라 **`tools/build-activity.py` 의 `PAGES`** 입니다.
  거기서 `tbd=True` 인 항목이 딱지가 됩니다. 값을 채우고 `tbd=False` 로 바꾼 뒤
  `python tools/build-activity.py` → `python tools/build-static.py`.
- ⚠️ **지어낸 값을 넣지 마세요.** 확인 안 된 것은 딱지로 두는 편이 낫습니다.

---

## 5. 답을 기다리는 물음 (묵힌 채 넘어가지 마세요)

1. **좋은기업센터 링크** — 지금은 `http://csr.action.or.kr` 입니다. 이 주소도 HTTPS 는
   안 붙습니다(HTTP 는 200). `csrcenter.cafe24.com` 으로 되돌릴지, HTTPS 를 붙일지.
2. **`/57 예산 모니터링`** — 헤더는 활동 다섯으로, 홈·형제 목록은 넷으로 세고 있습니다.
   활동입니까, 게시판입니까? 예산감시 화면의 '지금' 칸은 `/27` 을 읽는데 그 아래
   '최근에 쓴 글' 은 `/57` 을 읽어 서로 어긋나 있습니다.
3. **캠페인 이름** — `2026 하반기 밑빠진 독상` 을 그냥 `밑빠진 독상` 으로 줄일지.
4. **일시후원 모금함** — 도너스에서 모금함을 열면 `DONATE_URLS.once` 한 줄만 바꿉니다.

---

## 6. 붙이기 전 점검표

- [ ] `python tools/check.py --net` (인증서는 못 잡습니다 — 1-1 참고)
- [ ] `python tools/build-activity.py` · `build-campaign-together.py` · `build-static.py`
      를 돌려 **생성물이 원본과 맞는지**(`--check`)
- [ ] `dist/` 에 `확인 필요` 가 남았는지
- [ ] 세 캠페인 주소가 브라우저에서 경고 없이 열리는지
- [ ] 헤더·푸터는 **반복 섹션에 따로** 붙습니다(화면마다 복사되지 않습니다)

⚠️ **캠페이너스에 붙인 뒤에는 고치기가 비쌉니다.** 화면 하나를 고치면 그 화면을
손으로 다시 붙여야 합니다. **한 화면에 관한 큰 수정은 붙이기 전에 몰아서 끝내세요.**
다만 **공통 상단·하단은 반복 섹션 한 곳**이라, 거기만 고치는 것은 한 번으로 끝납니다.

---

## 7. 되묻지 말 것 (이미 정해진 것)

- Netlify 자동배포 안 합니다. `DEPLOYMENT.md` 는 계획이 아니라 기록입니다.
- 밑빠진 독상 페이지 겉모습은 손대지 않습니다.
- 페이지마다 다르게 디자인하지 않습니다. 틀은 하나입니다.
- 새 서비스에 가입시키지 않습니다(제보 = 구글 시트).
- ✖ **서명 화면은 2026-09-10 에 지웠습니다.** 쓰지 않기로 했고 접수는 다른
  형태로 해결됐습니다. **다시 제안하지 마세요** — 필요해지면 그때 다시 만듭니다.
- 개인정보처리방침 셋(`PRIVACY.md` · `PRIVACY-dok.md` · `PRIVACY-pb.md`)은
  **각각 완전체**로 둡니다. 합치지 않습니다. 관리자 승인 대기 중입니다.
- 상담소 `.cyc` 다섯 딱지와 걸어온 길 맺음말 세 마디는 꾸밈이 아닙니다
  (`HANDOFF_PB_2026-09-08.md` 5장).
