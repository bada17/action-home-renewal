# 내용과 디자인을 가르는 규칙 — `window.AH_DATA`

2026-08-31 사용자 결정에 따른 문서입니다.

> "사진이나 글을 적는 게 나 혼자 하는 게 아니야. 다른 사람도 함께 하기 때문에
> 디자인 요소가 전체가 코드면 안 돼."

**판단 기준 하나: 여러 사람이 손대는 내용은 코드 안에 두지 않는다.**

---

## 원칙: 캠페이너스가 이미 갖고 있는 기능은 새로 만들지 않는다

2026-08-31 사용자: **"캠페이너스 기능을 사용할 건 사용하게 만들어야 한다고."**

새로 지으면 우리가 평생 고쳐야 하고, 캠페이너스 것을 쓰면 캠페이너스가 고쳐 줍니다.
**무언가 만들기 전에 캠페이너스에 이미 있는지부터 확인하세요.**

| 기능 | 어떻게 | 확인한 날 |
|---|---|---|
| **통합검색** | `GET /search?keyword=<검색어>` | 2026-08-31 |
| 게시판 글 목록 | `action.or.kr/rss` (사진은 안 옵니다) | 2026-08-27 |
| **입력폼** | 신청·설문·의견을 받는 위젯. 응답은 관리자 **입력폼 관리**에서 보고 **일괄 다운로드** | 2026-08-31 |
| **서명** | 캠페이너스에는 없습니다. **캠페인즈**(`campaigns.kr`)에서 만들고 코드 위젯으로 현황판을 붙입니다 | 2026-08-31 |
| 자동 알림 | 입력폼이 들어오면 메일·문자·카카오 알림톡 발송 | 2026-08-31 |

### 입력폼 — 확인한 내용 (2026-08-31 도움말 센터에서 확인)

제보 · 서명 · 상담소 질문을 받을 그릇을 **새로 만들 필요가 없습니다.**
캠페이너스 소개 문구 그대로: “신청을 받고 지지자 데이터를 모으는 입력폼 —
구글 설문지보다 예쁘고 똑똑한 입력폼 페이지를 만들어보세요.”

- 받을 수 있는 항목: **단답형 · 여러 줄 · 단일 선택(라디오) · 체크 박스 ·
  목록 선택(셀렉트) · 파일 업로드**
- **개인정보 수집동의** 항목이 따로 있습니다 → 서명처럼 이름·연락처를 받아도 법적 요건을 채웁니다
- 응답은 **컨텐츠 관리 > 입력폼 관리**에서 보고 **골라서 일괄 다운로드**할 수 있습니다
  (게시판 · 지도 · 입력폼을 한 곳에서 관리하는 구조)
- 입력폼이 들어오면 **메일 · 문자 · 카카오 알림톡**을 자동으로 보낼 수 있습니다

⚠️ 대신 **그 칸의 겉모습은 캠페이너스가 정합니다**(아래 '왜 기본 위젯을 그대로 안 쓰나' 참고).
  글 목록과 달리 폼은 개인정보 · 동의 · 명단 관리가 딸려 오므로, 겉모습을 조금 양보하더라도
  **입력폼을 쓰는 편이 낫다**고 봅니다. 우리가 지으면 그 데이터를 우리가 평생 책임져야 합니다.

⚠️ 요금제에 따라 쓸 수 있는 위젯이 다를 수 있습니다 — **관리자에서 GPT 가 확인해 주어야 합니다.**

### 서명 — 캠페이너스가 아니라 캠페인즈입니다 (2026-08-31 확인)

캠페이너스 자체에는 서명 기능이 없습니다. 같은 회사(누구나데이터)의 **캠페인즈**
(<https://campaigns.kr/>)에서 서명·촉구 캠페인을 만들고, 그 **참여 현황판**을
캠페이너스 **코드 위젯**에 캠페인 번호를 넣어 붙입니다.
도움말 원문: “캠페인즈의 **서명과 촉구 캠페인만** 임베드 할 수 있습니다.”
위젯 높이는 내용에 따라 저절로 맞춰집니다. 캠페인 주소는 `campaigns.kr/campaigns/<번호>` 꼴입니다.

→ **명단을 우리가 갖고 싶으면 입력폼, 확산과 실시간 참여 수가 목적이면 캠페인즈**입니다.
  참여연대가 이 방식으로 잠재 지지자 11만 명을 모았다는 사례가 캠페이너스 쪽에 올라와 있습니다.

---

### 통합검색 — 확인한 내용

실제 `action.or.kr` 헤더의 폼에서 그대로 가져왔습니다:

    <form action="/search" method="get">
      <input name="keyword">

'한강버스'로 넣어 보니 관련 글이 제대로 나왔습니다(주민감사청구 기자회견,
긴급토론회, 감사원 감사 결과 등). 결과 화면은 캠페이너스가 자바스크립트로 그립니다.

→ 그래서 **검색 결과 화면을 우리가 만들 필요가 없습니다.**
  헤더의 검색칸(`tools/parts/header.html` 의 `.hd-search` · `.msearch`)이 그리로 보냅니다.
  ⚠️ `action="/search"` 와 `name="keyword"` 를 바꾸지 마세요. 이름이 다르면 검색어가 안 넘어갑니다.
  ⚠️ 미리보기에서는 안내 화면만 뜹니다. 진짜 결과는 캠페이너스에서 납니다.

---

## 누가 무엇을 하나

| | 담당 | 하는 일 |
|---|---|---|
| **틀 · 데이터 형식** | Claude | 코드에서 내용을 빼내고, 값이 들어올 자리와 형식을 정한다 |
| **캠페이너스 게시판 연결** | **GPT** | 게시판에서 글을 받아 아래 형식으로 `window.AH_DATA` 를 채운다 |

⚠️ Claude 는 게시판을 불러오는 코드를 짓지 않습니다. 형식만 맞춰 두고 기다립니다.

---

## 어떻게 이어지나

각 화면(캠페이너스 코드 위젯)은 그려지기 직전에 `window.AH_DATA` 를 한 번 봅니다.

    [캠페이너스 게시판]  --(GPT가 받아옴)-->  window.AH_DATA  --(이 저장소 코드가 그림)-->  화면

- **값이 있으면** 그 값으로 화면을 다시 그립니다.
- **값이 없으면** HTML 에 적힌 것을 그대로 둡니다. 게시판이 끊겨도 화면이 안 비어 보입니다.

즉 HTML 에 남아 있는 글·사진은 **씨앗(기본값)** 이지 진짜 내용이 아닙니다.
게시판이 붙으면 게시판이 이깁니다.

### GPT 가 붙일 자리

코드 위젯보다 **먼저** 실행되기만 하면 어디든 됩니다.

    <script>
    window.AH_DATA = {
      news:       [ ... ],
      issues:     [ ... ],   // 진행 중
      issuesDone: [ ... ],   // 종료 — 캠페인 목록 화면에만 씁니다
      activity: { local: {...}, power: {...}, budget: {...}, civic: {...} }
    };
    </script>

없는 열쇠는 그냥 빼면 됩니다. `news` 만 채우면 소식 칸만 바뀝니다.

---

## 형식

### `news` — 홈 '소식' 사진 카드 (최대 8)

    { title: '[논평] 미래대응기금...',        // 글 제목
      url:   '/27/?idx=173415033&bmode=view', // 글 주소
      date:  '2026.08.25',                    // 화면에 그대로 찍힙니다
      board: '일반 활동',                      // 게시판 이름. 뱃지로 뜹니다
      img:   'https://cdn.imweb.me/...png' }  // 없으면 색 타일 + 큰 글자

⚠️ **`action.or.kr/rss` 에는 사진이 없습니다**(title·link·description·pubDate 뿐).
   사진은 게시판 목록이나 캠페이너스 쪽 다른 경로에서 따로 가져와야 합니다.
   못 가져오면 `img` 를 빼세요 — 색 타일로 채워져 카드 무게는 유지됩니다.

### `issues` — 홈 첫 화면 + 캠페인 목록 (최대 5)

    { title: '더 걷힐 세금, 어디에 쓸래?',
      lead:  '추가세수 100조의 쓰임을 시민이 직접 고르고...',  // 한 줄 설명
      url:   '/27/?idx=173013295&bmode=view',
      img:   'https://cdn.imweb.me/...png',   // 없으면 mark 로 대체
      mark:  '상담',                           // 사진 없을 때 타일에 깔 큰 글자 (두 글자)
      tile:  '#0079a6',                        // 사진 없을 때 타일 색
      cat:   '예산감시',                       // 알약. 이 캠페인이 속한 활동 이름
                                             // (지자체 감시·권력감시·예산감시·시민참여·밑빠진 독상)
                                             // 없으면 알약 자체가 안 나옵니다
      due:   '2026-08-31' }                    // 없어도 됩니다

**캠페인 = 끝나는 날짜가 있는 것.** 규칙은 그대로입니다.
(2026-08-31 에 화면에 보이는 이름을 '이슈' → '캠페인' 으로 바꿨습니다. 코드 안 이름 `issues` 와 파일 `issue.html` 은 그대로입니다.)

### `issuesDone` — 캠페인 목록의 '종료' 탭

`issues` 와 같은 모양이고, 두 가지만 다릅니다.

    { title: '한강버스 주민감사청구',
      lead:  '경제성 근거 없이 추진된 사업에 주민감사를 청구했습니다.',
      url:   '/27/?idx=172916152&bmode=view',
      img:   'https://cdn.imweb.me/...png',
      when:  '2026년 8월 종료' }   // 화면에 그대로 찍힙니다. due 가 아닙니다

- `due`(마감일) 대신 **`when`**(언제 끝났나)을 씁니다.
- `cat`(활동 이름)은 이 탭에서 안 씁니다.

**홈 첫 화면에는 안 올라갑니다.** 끝난 것을 첫 화면에 두면 지금 참여할 수 있는 것이 밀립니다.
탭에 붙은 개수(`진행 중 5`)는 넘긴 줄 수로 저절로 고쳐집니다.

---

#### 값을 넘길 때 지켜야 할 것 두 가지

1. **엔티티는 풀어서 넘기세요.** RSS 는 주소를 `...&amp;bmode=view` 로 줍니다.
   그대로 넘기면 화면 코드가 한 번 더 감싸 `&amp;amp;` 가 되어 **링크가 깨집니다.**
   받는 쪽에서 `html.unescape` 한 번 돌린 값을 넘기세요.
   (화면 코드는 받은 값을 알아서 안전하게 감쌉니다. 미리 감싸 두지 마세요.)

2. **날짜는 화면에 그대로 찍힙니다.** RSS 의 `Tue, 25 Aug 2026 ...` 를 그대로 넘기면
   그 글자가 그대로 뜹니다. **`2026.08.25` 형태로 바꿔서** 넘기세요.

---

## `activity.<열쇠>` — 활동 다섯 장

열쇠는 `local`(지자체) · `power`(권력) · `budget`(예산) · `civic`(시민참여).
밑빠진 독상은 여기 없습니다 — `awards.json` 을 따로 받아 그리고, 겉모습은 손대지 않기로 확정.

    { headline: '지방의회, 지켜보고 있습니다',
      lede:     '우리 동네 예산과 조례가 어떻게 정해지는지...',

      nums: [ { n: '17', unit: '곳', label: '감시한 지자체 수' }, ... ],   // 최대 3

      now:  [ { t: '2026년 서울시 추경 심사 모니터링',   // 지금 보고 있는 것
                due: '9월 심사', href: '...', img: '...', mark: '추경' } ],

      result: [ { t: '한강버스 사업 경제성 검증',        // 사건 하나
                  did: '무엇을 했나 (한 줄)',
                  got: '무엇이 바뀌었나 (한 줄)',
                  when: '2026.06 - 2026.08',
                  src: [ ['감사 결과 정리', '/27/?idx=...'] ] } ],

      posts:  [ { title: '...', date: '2026.07.03', url: '...' } ],  // 글 목록

      photos: [ { src: '...', alt: '...', t: '제목', d: '한 줄' } ] } // 0장이면 칸이 안 나옴

**`sig`(활동마다 하나뿐인 고유 칸 — 지역 태그·문답·달력·문)는 아직 코드에 둡니다.**
구조가 활동마다 달라 게시판 글 한 편으로 담기 어렵습니다. 여기까지 게시판으로 뺄지는
나머지가 돌아가는 것을 보고 정합니다.

---

## '페이지 문구' 게시판 (2026-08-31 사용자 선택)

`headline` · `lede` · `nums` · `result` 처럼 **글 목록이 아닌 값**은
캠페이너스 게시판을 하나 더 파서 **글 한 편 = 화면 한 장**으로 둡니다.
편집 창구가 게시판 하나로 통일되고, 디자인은 코드가 계속 쥡니다.

권하는 글 형식 (사람이 쓰기 쉬운 쪽으로 — 실제 파싱은 GPT 가 정하면 됩니다):

    제목: 활동 > 지자체 감시

    큰제목: 지방의회, 지켜보고 있습니다
    한줄: 우리 동네 예산과 조례가 어떻게 정해지는지, 의회가 제 역할을 하는지 기록합니다.

    숫자: 17 | 곳 | 감시한 지자체 수
    숫자: 4 | 건 | 올해 낸 의견서

    결과: 한강버스 사업 경제성 검증
    했다: 경제성 근거가 공개되지 않은 채 추진되던 사업을 짚었습니다.
    됐다: 감사원 감사에서 경제성 B/C 0.17 이 확인됐습니다.
    때: 2026.06 - 2026.08

빈 줄로 끊고, 없는 줄은 그냥 안 쓰면 됩니다.

---

## 왜 캠페이너스 기본 위젯을 그대로 안 쓰나

캠페이너스 게시판 위젯을 화면에 그대로 앉히면 편집은 제일 쉽지만,
**그 칸의 겉모습을 캠페이너스가 정합니다.** 화면 아홉 장을 한 벌로 맞춰 온
`DESIGN.md` 의 규칙이 그 칸에서만 끊겨서, "같은 단체가 만든 화면"으로 안 읽힙니다.

지금 방식은 **글 쓰는 사람 입장에서 난이도가 똑같습니다** — 캠페이너스 관리자에서
평소처럼 글을 올리면 됩니다. 다른 점은 그 글을 누가 그리느냐뿐입니다.

---

## 지금 어디까지 됐나

| 화면 | 자리 | 비고 |
|---|---|---|
| 홈 — 소식 카드 | ✅ | `AH_DATA.news` |
| 홈 — 첫 화면 이슈 | ✅ | `AH_DATA.issues` |
| 캠페인 목록 `issue.html` | ✅ | `AH_DATA.issues` · `AH_DATA.issuesDone` |
| 활동 4장 | ✅ | `AH_DATA.activity.<열쇠>` · `sig` 칸만 코드에 남음 |
| 밑빠진 독상 화면 | — | 겉모습을 손대지 않기로 확정. `awards.json` 을 그대로 씁니다 |
| 자료실 `library.html` | ✅ | 이미 `action.or.kr/rss` 에서 받아 만듭니다 |
| 밑빠진 독상 | ✅ | 이미 `awards.json` 을 받아 그립니다 |
