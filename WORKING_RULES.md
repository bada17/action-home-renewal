# 협업 작업 규칙

1. 기본 작업 대상은 이 저장소의 `index.html`입니다.
2. 기존 `action.or.kr` 공개 홈페이지와 캠페이너스 데이터는 읽기 전용으로 취급합니다.
3. 캠페이너스 반영은 숨김·미게시 페이지 `/69`에서만 진행합니다.
4. 사용자의 명시적 승인 없이는 게시하거나 대표 홈으로 전환하지 않습니다.
5. 기존 콘텐츠의 삭제 요청은 새 화면에서의 제외로 해석하며, 원본 데이터와 URL은 보존합니다.
6. 디자인 확정 전까지 코드 시안으로 작업하고, 확정 후 기본 위젯 중심으로 다시 조립합니다.
7. **개편에서 새로 짓지 않은 목차는 캠페이너스에 있는 지금 디자인을 그대로 씁니다**
   (2026-08-31 사용자 결정: "지금 아예 손대지 않는 목차들은 기존에 있는 디자인
   그대로 사용할거야. 나중에 여유 있을때 수정하는 걸로 하지뭐").
   → 우리가 만든 화면은 아홉입니다: 홈 · 활동 다섯 · 캠페인 목록 · 참여예산 상담소 · 자료실.
   → 나머지는 **손대지 않습니다.** 헤더에서 링크만 걸고 겉모습은 캠페이너스 것 그대로 둡니다:
     소개(`/aboutus` `/45` `/31` `/32` `/53`) · 후원(`/35` `/36` `/67`) ·
     소식의 공지(`/26`) · 발행물(`/23`) · 뉴스룸(`/24`) · 연대 활동(`/54`) · 투명성(미정).
   → 그래서 이 화면들이 우리 디자인과 달라 보이는 것은 **결함이 아니라 지금의 결정**입니다.
   통일은 나중에 여유가 생기면 합니다. 미리 손대지 말 것.

   → 2026-09-02 메뉴 결정: `예산 모니터링(/57)`은 활동에, `회원 총회(/35)`는
     후원하기에 둡니다. 회원 공간(`/40`)·이슈 토론(`/41`)·회원 활동(`/48`)은
     삭제 예정이며 새 목차에는 넣지 않습니다. 기존 후원 페이지(`/join`)는 도너스
     이관 중이라 사용하지 않고, 새 도너스 주소가 나오기 전까지 주요 후원 버튼은
     `정기후원하기`의 mrmweb 신청서로 연결합니다.

   → 2026-09-02 캠페인 페이지 연결: `밑빠진 독상(/76)` · `함께하기(/77)` ·
     `2026 하반기 밑빠진 독상(/78)` · `10만원 예산편성(/79)` ·
     `참여예산 상담소(/80)`. `/76`의 메뉴 이름은 `밑빠진 독상`으로만 씁니다.

8. **Netlify 자동배포는 하지 않습니다 — 할 일 목록에 없습니다** (2026-09-01 사용자 결정).
   GPT 가 보낸 사양(저장소 → Netlify 자동배포 + 캠페이너스 외부 로더)은 검토했고
   **하지 않기로 했습니다.** 이유는 `DEPLOYMENT.md` 에 있습니다.
   → 배포는 지금처럼 **GPT 가 코드 위젯에 붙여넣습니다.**
   → 나중에 필요해지면 그때 다시 이야기할 일이지, 지금 남아 있는 일이 아닙니다.
   → **이것으로 다시 제안하지 마세요.**

9. **캠페인은 캠페이너스 게시판에서 받습니다 — '진행 중' 과 '종료' 두 개**
   (2026-09-01 사용자 결정: "캠페인에 입력하고 등록하면 메인화면에 뜨게 만들면 되잖아").
   글 하나 = 캠페인 하나. 등록은 글 쓰기, 내리기는 글 옮기기입니다.
   두 게시판은 상단 하위 메뉴에 각각 노출하지 않고, `함께하기(/77)`에서
   `진행 중`·`종료` 탭의 데이터 원본으로 사용합니다.
   → 2026-09-02 확정 번호: `진행 중(/81)` · `종료(/82)`. `/81`은 홈과 `/77`의
     진행 중 탭, `/82`는 `/77`의 종료 탭으로 `action.or.kr/rss`에서 자동 연결합니다.
   → **홈 첫 화면과 캠페인 목록이 같은 값 하나(`AH_DATA.issues`)를 씁니다.** 한 곳만 채우면 둘이 채워집니다.
   → 형식과 남은 구멍(한 줄 설명·바깥 링크)은 `CONTENT.md` 의 '캠페인을 게시판에서 받기' 를 볼 것.

10. **활동 게시판은 기본 게시판 위젯을 보존합니다.** 2026-09-02에는 지자체 감시(`/49`)만
    먼저 시험합니다. `activity-local-before-board.html` → 기존 `/49` 기본 게시판 위젯
    순서로 두며 별도 `after` 코드는 쓰지 않습니다. 미리보기용 `activity-local.html` 전체를
    캠페이너스에 붙이지 않습니다. 밑빠진 독상(`/76`)은 이 분리 대상이 아닙니다.
    `/49` 게시판의 `No`·`글쓴이` 열은 캠페이너스 게시판 디자인 설정에서 끕니다.
    앞 코드의 CSS는 게시글 제목을 굵게 표시하고, 표의 열 너비를 넓은 화면
    `10/75/15`, 좁은 화면 `14/61/25`(카테고리/제목/작성시간)로 맞춥니다.
    활동 본문과 게시판의
    폭·간격은 내부 요소가 아니라 바깥 컨테이너를 `1120px` 기준으로 맞출 것.
