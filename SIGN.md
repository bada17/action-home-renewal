# 서명 받기 — 구글 시트로

서명을 우리 화면에서 받고, 명단은 **구글 시트**에 쌓습니다.
새로 가입할 곳은 없습니다. 쓰던 구글 계정 하나면 됩니다.

```
[우리 서명 화면]  --보냄-->  [구글 Apps Script 웹 앱]  --적음-->  [구글 시트]
      sign.html                  tools/apps-script/sign.gs           명단
         ^                                  |
         +------- 지금 몇 명인지 숫자만 ------+
```

## 왜 이렇게 하나

- 캠페이너스에는 **서명 기능이 없습니다.** 자매 서비스 캠페인즈로 하는 것이 공식
  방법인데 쓸 수 없다고 하셔서 자체로 만들었습니다.
- 캠페이너스 **입력폼**으로 받을 수도 있지만, 입력폼은 **"지금 몇 명이 서명했는지"를
  화면에 못 띄웁니다.** 서명은 그 숫자가 곧 힘이라 자체로 만드는 값어치가 있습니다.
- 구글 폼을 그대로 붙이면 **그 칸만 구글 디자인**이 되어 화면 아홉 장과 따로 놉니다.

---

## 켜는 법 (10분, 한 번만)

### 1. 시트 만들기

구글 드라이브에서 **새 스프레드시트**를 하나 만듭니다. 이름은 「시민행동 서명」 정도로.
칸은 만들지 않아도 됩니다 — 첫 서명이 들어오면 스크립트가 알아서 만듭니다.

⚠️ **이 시트는 공유하지 마세요.** 이름·이메일·전화번호가 쌓이는 곳입니다.
   담당자만 열 수 있게 두고, '링크가 있는 모든 사용자'는 절대 켜지 마세요.

### 2. 코드 붙이기

그 시트에서 **확장 프로그램 › Apps Script** 를 엽니다.
기본으로 들어 있는 `myFunction` 을 다 지우고,
이 저장소의 **`tools/apps-script/sign.gs`** 내용을 통째로 붙여 넣고 저장합니다.

### 3. 웹 앱으로 배포

오른쪽 위 **배포 › 새 배포**

| 칸 | 고를 것 |
|---|---|
| 유형 | **웹 앱** |
| 실행 사용자 | **나** (시트에 적는 것은 내 권한으로) |
| 액세스 권한 | **모든 사용자** (서명하는 시민은 구글 로그인이 없습니다) |

처음 한 번은 구글이 권한을 묻습니다. '고급 › 안전하지 않은 페이지로 이동'을 거쳐
허용하면 됩니다. 내가 만든 스크립트라 그렇게 뜹니다.

끝나면 **웹 앱 URL** 이 나옵니다. `https://script.google.com/macros/s/…/exec` 꼴입니다.

### 4. 주소를 화면에 넣기

`sign.html` 아래쪽 스크립트에서 이 한 줄을 찾아 그 주소를 넣습니다.

```js
var SIGN_API = '';        // ← 여기에 붙여넣기
```

넣는 순간 살아납니다. 비어 있으면 화면 맨 위에 주황 띠가 뜨고 **제출이 막힙니다** —
저장되지 않는 폼에 개인정보를 적게 두면 안 되기 때문입니다.

### 5. 확인

미리보기에서 `/sign` 을 열고 시험 삼아 한 번 서명해 봅니다.
시트에 줄이 생기고, 화면의 숫자가 1 이 되면 된 것입니다.
시험한 줄은 시트에서 지우세요.

---

## 고칠 때

| 고치고 싶은 것 | 어디를 |
|---|---|
| 제목 · 설명 · 왜 서명하나 | `sign.html` 의 `.top` 과 `.why` |
| 목표 인원 | `<div id="sign" … data-goal="1000">` |
| 캠페인 구분 | 같은 줄의 `data-c="tax-2026"`. **캠페인마다 다른 값**을 주면 한 시트에 나눠 쌓입니다 |
| 동의 문구 · 보유기간 | `sign.html` 의 `<details>` 안. 고쳤으면 `sign.gs` 의 `CONSENT_VERSION` 날짜도 올리세요 |
| 받는 항목 | `sign.html` 의 `.f` 칸과 `sign.gs` 의 `HEADERS` · `accept_()` |

⚠️ `sign.gs` 를 고친 뒤에는 **배포 › 배포 관리 › 수정 › 버전 '새 버전'** 으로 다시
배포해야 반영됩니다. 주소는 그대로입니다.

---

## 명단 내려받기

시트에서 **파일 › 다운로드 › Microsoft Excel(.xlsx)** 또는 **CSV**.
내려받은 파일도 똑같은 개인정보입니다. 바탕화면에 두지 마세요.

---

## 아직 정해야 하는 것

화면에 `확인 필요` 딱지로 남겨 두었습니다. **공개 전에 하나도 남으면 안 됩니다.**

1. **보유기간** — 언제까지 갖고 있다가 지울지. 동의문에 적어야 합니다.
2. **어디에 전달하나** — 기관 이름.
3. **언제까지 모으나** — 마감.
4. 서명 제목과 요구안 문구.

## 한계 (알고 쓰세요)

- **응답이 조금 느립니다.** 구글 쪽에서 0.5~2초 걸립니다. 버튼이 '보내는 중…'으로 바뀝니다.
- **막을 수 있는 스팸은 초보 수준입니다.** 사람 눈에 안 보이는 칸(허니팟)과
  너무 빨리 채운 것을 거릅니다. 작정하고 넣는 자동 프로그램은 못 막습니다.
  서명 수가 갑자기 튀면 시트를 눈으로 확인하세요.
- **같은 캠페인에 같은 이메일은 한 번만** 들어갑니다. 두 번째부터는 조용히 넘어갑니다.
- **전화번호는 글자로 저장됩니다**(앞의 0 이 사라지지 않게).
- 구글 계정 하나가 하루에 처리할 수 있는 양에 한도가 있습니다. 수천 명 규모까지는
  넉넉하고, 그 이상이 예상되면 클라우드플레어(D1)로 옮기는 편이 낫습니다 —
  밑빠진 독상 저장소에 같은 일을 하는 코드가 이미 있습니다(`functions/api/join.js`).
