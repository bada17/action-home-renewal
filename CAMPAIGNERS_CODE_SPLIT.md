# 캠페이너스 코드 분리 지도

> **Claude와 GPT가 캠페이너스 페이지 코드를 고치기 전에 먼저 보는 기준 문서입니다.**
> 대화 기억에 기대지 않습니다. 페이지 번호, 게시판 번호, 파일 이름이나 배치 순서가
> 바뀌면 코드와 함께 이 문서도 고칩니다.

## 한 줄 원칙

**페이지·게시판마다 자기 코드 조각을 따로 둡니다.** 여러 페이지의 코드를 한 파일로
복사해 관리하지 않으며, 공통 상단·공통 하단만 모든 페이지가 같은 원본을 씁니다.

생성 파일에는 붙여넣기 범위를 나타내는 `CAMPAIGNERS:... START/END` 표시가 있습니다.
생성 파일을 직접 고치지 말고 아래 표의 **원본**을 고친 뒤 생성기를 다시 실행합니다.

## 모든 페이지의 공통 뼈대

```text
공통 상단
→ 그 페이지 전용 코드 조각 또는 기본 게시판
→ 필요한 경우 페이지 전용 하단 조각
→ 공통 하단
```

- 공통 상단 원본: `tools/parts/header.html`
- 공통 하단 원본: `tools/parts/footer.html`
- 상단·하단을 각 페이지 코드에 복사하지 않습니다.
- 캠페이너스에서는 공통 상단·하단을 반복 섹션의 별도 코드 위젯으로 관리합니다.

## 활동 — 기본 게시판을 보존하는 세 페이지

| 화면 | 페이지·게시판 | 캠페이너스 배치 순서 | 수정 원본 | 생성 파일 |
|---|---:|---|---|---|
| 지자체 감시 | `/49` | 앞 코드 → `/49` 기본 게시판 → 뒤 코드 → 공통 하단 | `tools/build-activity.py` | `activity-local-before-board.html` · `activity-local-after-board.html` |
| 예산감시 | `/27` | 앞 코드 → `/27` 기본 게시판 → 뒤 코드 → 공통 하단 | `tools/build-activity.py` | `activity-budget-before-board.html` · `activity-budget-after-board.html` |
| 시민참여 | `/51` | 앞 코드 → `/51` 기본 게시판 → 뒤 코드 → 공통 하단 | `tools/build-activity.py` | `activity-civic-before-board.html` · `activity-civic-after-board.html` |

활동 세 페이지의 규칙:

- 뒤 코드는 게시판 아래의 `시민행동이 하는 다른 일`만 담습니다. 사이트 공통 하단과
  합치지 않습니다. 활동 페이지마다 자기 활동을 제외한 링크가 달라 페이지별 파일을 씁니다.
- `activity-local.html`, `activity-budget.html`, `activity-civic.html`은 로컬 미리보기용
  전체 화면입니다. 캠페이너스에 통째로 붙이지 않습니다.
- 코드로 만든 최근 글 목록은 넣지 않습니다. 해당 페이지의 기본 게시판이 글 목록,
  검색, 페이지 나눔과 글쓰기를 맡습니다.
- 활동 앞 코드의 게시판 CSS는 세 페이지가 `tools/build-activity.py`의 `BOARD_CSS`를
  공유합니다. 한 페이지만 복사해 따로 고치지 않습니다.
- 활동 뒤 코드의 CSS는 `tools/build-activity.py`의 `AFTER_BOARD_CSS`를 공유합니다.
- 밑빠진 독상(`/76`)은 이 기본 게시판 분리 구조에서 제외합니다.

활동 생성:

```text
python tools/build-activity.py
```

## 캠페인 — 함께하기 허브

`함께하기(/77)`는 기본 게시판 위젯을 화면에 직접 넣는 페이지가 아닙니다. 관리용
게시판 두 개를 `/rss`에서 읽어 캠페인 카드로 보여 줍니다.

| 역할 | 번호 | 쓰이는 곳 |
|---|---:|---|
| 진행 중 캠페인 원본 게시판 | `/81` | 홈 첫 화면 + `/77` 진행 중 탭 |
| 종료 캠페인 원본 게시판 | `/82` | `/77` 종료 탭 |

`/77`의 코드 위젯 배치 순서:

```text
campaign-together-layout.html
→ campaign-together-scripts.html
→ 공통 하단
```

- 첫 조각은 CSS와 화면 HTML입니다.
- 둘째 조각은 `/81·/82` RSS 연결, 카드 렌더링, 진행 중·종료 탭과 날짜 계산입니다.
- 두 조각 사이에 `/81` 또는 `/82` 기본 게시판 위젯을 넣지 않습니다.
- 게시글 제목 맨 앞의 `[링크:https://외부주소]` 또는 `[링크:/내부주소]`를 카드
  목적지로 씁니다. 화면에는 표시하지 않으며, 표시가 없거나 주소가 안전하지 않으면
  해당 게시글로 이동합니다. 자세한 작성 형식은 `CONTENT.md`를 봅니다.
- 외부 주소는 `↗`를 표시하고 새 탭으로 엽니다. 내부 주소와 원래 게시글은 같은 탭으로
  엽니다.
- `/77`의 수정 원본은 `issue.html` 한 곳입니다.
- 분리 파일은 `tools/build-campaign-together.py`로 다시 만듭니다.

함께하기 생성과 확인:

```text
python tools/build-campaign-together.py
python tools/build-campaign-together.py --check
```

개별 캠페인(`/78`, `/79`, `/80`)은 `/77` 안에 합치지 않습니다. 각자 자기 캠페이너스
페이지와 코드 조각을 유지합니다. 현재 저장소에서 참여예산 상담소(`/80`)의 원본은
`pb.html`입니다.

## 수정할 때 지키는 순서

1. 이 문서에서 대상 페이지·게시판 번호와 원본 파일을 확인합니다.
2. 생성 파일이 아니라 원본 또는 생성기를 고칩니다.
3. 대상 생성기를 실행합니다.
4. 생성 파일의 `CAMPAIGNERS:... START/END` 범위만 해당 코드 위젯에 붙입니다.
5. 기본 게시판을 쓰는 페이지는 코드 조각과 게시판 위젯의 순서를 다시 확인합니다.
6. PC 1440px, 모바일 390px에서 폭·간격·기능을 확인합니다.
7. 검색, 페이지 나눔, 행 클릭, 진행 중·종료 탭 등 게시판 기능도 확인합니다.
8. 변경 내용을 `git diff`로 확인합니다. 사용자가 요청하기 전에는 커밋·푸시하지 않습니다.

## 새 페이지나 게시판을 추가할 때

- 기존 생성 파일을 복사해 새 원본으로 삼지 않습니다.
- 먼저 이 문서 표에 페이지 번호, 게시판 번호, 원본, 생성 파일과 배치 순서를 추가합니다.
- 한 페이지의 수정이 다른 페이지 생성 파일을 뜻하지 않게 바꾸지 않도록 생성 대상과
  이름을 명시합니다.
- 공통으로 고쳐야 하는 CSS·스크립트만 생성기 또는 공통 부품으로 올립니다.
