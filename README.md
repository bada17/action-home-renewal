# 함께하는 시민행동 홈페이지 개편

> 작업 규칙은 **`AGENTS.md`** 에 있습니다. Claude 와 GPT 는 폴더를 열 때 그 파일을 자동으로 읽습니다.
> 기록은 이 `README.md` 한 곳에만 남기고, 지나간 것은 딱지를 붙이지 말고 지웁니다.

`action.or.kr` 홈페이지 개편 시안. 화면을 **캠페이너스 코드 위젯에 붙이는** 구조입니다.

---

## 지금 상태 (2026-09-11)

| | |
|---|---|
| 저장소 | `bada17/action-home-renewal` **공개**, 기본 브랜치 `main` |
| 로컬 | `…\밑빠진독상 페이지\action-home-renewal` (GPT 와 **같은 폴더**) |
| 검토 사이트 | <https://bada17.github.io/action-home-preview/> — `main` 에 push 하면 자동 |
| 공개 여부 | **아직 아닙니다.** 캠페이너스 숨김 페이지 `/69`, 대표 홈 전환 안 켬 |
| 후원 | 도너스 `donus.org/withaction0909` (옛 mrmweb 은 다 걷어냄) |

**화면 작업은 사실상 끝났습니다. 남은 것은 캠페이너스 이식입니다.**

⚠️ 저장소가 공개입니다 — 회원·후원자 정보, 관리자 계정, 열쇠는 **절대** 올리지 않습니다.

**세 캠페인 사이트** (2026-09-11 전부 200, 인증서 정상)

- 밑빠진 독상 `https://dok.action.or.kr` — 깃허브 페이지
- 시민참여 상담소 `https://pb.action.or.kr` — 깃허브 페이지, 원본은 `participatory-budget` 저장소
- 10만원 예산편성 투표 `https://vote.action.or.kr`

`*.pages.dev` 주소는 어디에도 걸지 않습니다.

---

## 캠페이너스에 붙이는 구조 — 제일 중요

**각 페이지는 개별 코드 위젯 하나, 상단과 하단은 각각 반복 섹션 한 곳.**

- `index.html` · `activity-*.html` · `issue.html` · `dok-history.html` · `library.html` 은
  각각 **코드 위젯 하나에 파일 전체**를 붙입니다. 범위는 파일 안의
  `CAMPAIGNERS:PAGE-CODE START/END`.
- 페이지 파일에는 공통 상단·하단이 **일부러 없습니다.** `#ah-nav` 나 `#ah-foot` 를
  페이지 코드에 넣으면 반복 섹션과 겹쳐 두 번 나옵니다.
- **공통 상단 원본 = `tools/parts/header.html` 한 곳.** 파일 전체를 **페이지 맨 아래**
  코드 위젯에 붙이고 그 섹션을 `섹션 반복` 으로 설정합니다(반복은 맨 아래에서만 켜집니다).
  위젯은 아래 있어도 헤더는 화면 위에 고정됩니다.
- **공통 하단 원본 = `tools/parts/footer.html` 한 곳.** 기본 푸터 바로 위 코드 위젯에
  붙이고 역시 `섹션 반복`.
- 캠페이너스 기본 상단·푸터는 우리 코드가 숨깁니다. 하단 디자인을 또 만들지 않습니다.

⚠️ **하단(`footer.html`)은 상담소와 밑빠진 독상이 읽어 갑니다**(베끼지 않습니다).
하단을 고쳤으면 두 저장소에서 한 번씩 다시 구우세요.

    participatory-budget:  python build.py
    dokseong:              python tools/port-footer.py

**활동 셋은 게시판을 사이에 두고 나눠 붙입니다** — `/49` 지자체 감시 · `/27` 예산감시 ·
`/51` 시민참여. 순서는 `activity-<이름>-before-board.html` → 그 페이지 기본 게시판 위젯 →
`activity-<이름>-after-board.html` → 공통 하단. 미리보기용 전체 파일
(`activity-local.html` 등)을 붙이지 않습니다. `/76` 은 이 구조에서 제외.

자세한 배치 순서와 원본·생성물 구분은 **`CAMPAIGNERS_CODE_SPLIT.md`** 가 기준입니다.

⚠️ **붙인 뒤에는 고치기가 비쌉니다.** 화면 하나를 고치면 그 화면을 손으로 다시 붙여야
합니다. 큰 수정은 붙이기 전에 몰아서 끝내세요. 공통 상단·하단만 예외입니다(한 곳).

### 페이지 번호

`/69` 홈(숨김) · `/49` 지자체 감시 · `/27` 예산감시 · `/51` 시민참여 ·
`/76` 밑빠진 독상 · `/77` 함께하기 · `/81` 진행 중 캠페인 게시판 · `/82` 종료(메뉴에 안 냄).

**`/78 · /79 · /80` 은 만들지 않습니다** — 개별 캠페인은 위 외부 주소로 바로 갑니다.

---

## 직접 고치면 안 되는 파일

생성물입니다. 원본이나 생성기를 고치고 다시 구우세요.

| 생성물 | 생성기 |
|---|---|
| `activity-*.html` 넷 | `tools/build-activity.py` |
| `library.html` | `tools/build-library.py` |
| `donate-guide.html` | `tools/build-donate.py` (랜딩 저장소 `action-landing` 을 읽습니다) |
| `campaign-together-layout.html` | `tools/build-campaign-together.py` |
| `dist/` | `tools/build-static.py` |
| 개인정보처리방침 화면 둘 | `tools/build-privacy.py` |

---

## 남은 일

**사용자만 할 수 있는 것**

1. **캠페이너스에 붙이기** — 코드 위젯은 손으로 붙이는 구조입니다(섹션 반복도).
2. **활동 화면 셋의 내용 채우기** — 아래 「확인 필요」.

⚠️ **지금 캠페이너스에 붙어 있는 상단은 2026-09-07 이전 옛 코드입니다.** 화면에
로그인·회원가입이 남아 있으면 그것입니다. 상단 반복 섹션을 다시 붙이면 사라집니다
(로그인은 도너스 마이페이지로 옮겨 후원 목차의 '회원정보 수정' 이 됐습니다).

**GPT**

- `assets/donus/` 와 `tools/make-donus-logo-assets.py` 가 커밋 없이 놓여 있는데
  저장소 어디에서도 부르지 않습니다. 쓸 것이면 그 화면과 한 커밋으로, 아니면 지우세요.

### 「확인 필요」 딱지

`tools/check.py` 는 소스 기준 70곳이라 세지만, `build-static.py` 가 주석을 걷어내므로
**실제 화면에 나가는 것은 활동 화면 셋뿐**입니다 — `/27`(5) · `/49`(5) · `/51`(6).
일정·기간·결과 카드입니다. 홈 · 함께하기 · 후원 안내는 깨끗합니다.

고칠 곳은 화면 파일이 아니라 **`tools/build-activity.py` 의 `PAGES`** 입니다.
`tbd=True` 인 항목이 딱지가 됩니다. 값을 채우고 `tbd=False` 로 바꾼 뒤
`build-activity.py` → `build-static.py`.

⚠️ **지어낸 값을 넣지 마세요.** 확인 안 된 것은 딱지로 두는 편이 낫습니다.

---

## 답을 기다리는 물음

1. **좋은기업센터 링크** — 지금 `http://csr.action.or.kr`. HTTPS 가 안 붙습니다.
   `csrcenter.cafe24.com` 으로 되돌릴지, 인증서를 붙일지.
2. **`/57 예산 모니터링`** — 헤더는 활동 다섯으로, 홈·형제 목록은 넷으로 셉니다.
   활동입니까 게시판입니까? 예산감시 화면의 '지금' 칸은 `/27` 을, 그 아래
   '최근에 쓴 글' 은 `/57` 을 읽어 서로 어긋나 있습니다.
3. **캠페인 이름** — `2026 하반기 밑빠진 독상` 을 그냥 `밑빠진 독상` 으로 줄일지.
4. **감시사업 줄의 다음 걸음** — "감시 중"이라는 사실만 전하고 제보하기 같은 다음
   걸음이 없습니다. 문안은 사람이 정할 일입니다.

---

## 되묻지 말 것 (이미 정해진 것)

- **Netlify 자동배포 안 합니다.** `DEPLOYMENT.md` 는 계획이 아니라 기록입니다.
- **캠페인은 캠페이너스 게시판에서 받습니다** — 진행 중 `/81` · 종료 `/82`.
  글 하나 = 캠페인 하나. 제목 형식은 `CONTENT.md`.
- **홈 첫 화면과 캠페인 목록이 같은 값 하나(`AH_DATA.issues`)를 씁니다.** 한 곳만 채우면 둘이 찹니다.
- **개편에서 새로 짓지 않은 목차는 캠페이너스 지금 디자인을 그대로 씁니다.**
  그 화면들이 우리 디자인과 달라 보이는 것은 결함이 아니라 결정입니다.
- **페이지마다 다르게 디자인하지 않습니다.** 틀은 하나입니다.
- **밑빠진 독상 페이지 겉모습은 손대지 않습니다.**
- **새 서비스에 가입시키지 않습니다**(제보 = 구글 시트).
- ✖ **서명 화면은 2026-09-10 에 지웠습니다.** 다시 제안하지 마세요.
- **`/52`(활동 목차) 화면은 만들지 않습니다.** 상단 '활동'은 가는 곳이 아니라
  하위 목차를 여는 손잡이라 `header.html` 에서 href 를 뗐습니다.
- **개인정보처리방침 셋**(`PRIVACY.md` · `PRIVACY-dok.md` · `PRIVACY-pb.md`)은 각각
  완전체로 둡니다. 합치지 않습니다. 2026-09-10 에 관리자 회신본(14항) 한 벌로 갈았습니다.
  화면 둘은 손으로 옮기지 말고 `tools/build-privacy.py` 로 굽습니다. 상담소는 구운 뒤
  `pb-action-site/` 로 복사해 push 해야 사는 화면이 바뀝니다.
- **첫 화면 '밑빠진 독상' 줄 밑의 예산감시 사업**(`.lsub`)은 독상 접수처의
  `?action=campaigns` 를 읽어 스크립트가 그립니다 — **HTML 에 손으로 적지 마세요.**
  못 받으면 안 붙습니다. 접수처가 9.7~38.5초 걸려서 화면에 씨앗을 심어 뒀고, 받아 오면
  갈아 끼웁니다. 게시판 값으로 목록을 다시 그리면 지워지므로 `ah-campaigns-ready` 에서
  다시 붙입니다.

---

## 알려진 함정

- **RSS 주소의 `&amp;`** — `...&amp;bmode=view` 로 옵니다. 그대로 넘기면 한 번 더 감싸져
  `&amp;amp;` 가 되어 링크가 깨집니다. 엔티티를 풀어서 넘기세요.
- **RSS 날짜** — `Tue, 25 Aug 2026 …` 입니다. `2026.08.25` 로 바꿔서 넘기세요.
- **알약 목차는 `.is-hover`(스크립트)로 엽니다.** CSS `:hover` 로 되돌리지 마세요 —
  알약에서 판으로 커서를 내리는 사이에 닫혀 버립니다.
- **회원가입을 `/join` 으로 걸지 마세요.** 거기는 후원 화면입니다.
- **`tools/build-activity.py` 의 `AH_SCRIPT`** 가 만드는 조각은 `build_nums` ·
  `build_now` · `build_result` · `build_posts` · `build_photos` 가 만드는 것과 모양이
  같아야 합니다. 한쪽만 고치면 게시판을 물리는 순간 화면이 달라집니다.
  (스크립트를 `TEMPLATE` 안에 넣지 마세요 — `.format()` 문자열이라 중괄호를 두 번 써야 합니다.)
- **큰 칸 카드는 통째로 `<a>` 가 아닙니다.** 제목 링크(`.slide-go`)가 `::after` 로 카드를
  덮습니다. 그래야 카드 안에 다른 링크를 둘 수 있습니다.
- **`tools/check.py --net` 은 인증서 문제를 못 잡습니다**(`ssl.CERT_NONE`).
  HTTPS 는 `openssl s_client -connect <주소>:443 -servername <주소>` 로 직접 보세요.
- **좌우 여백은 48 / 36(820px) / 28(430px)**, 바깥 폭은 **1120px 가운데 정렬**.
  새 화면도 이 값을 씁니다. 음수 여백을 쓰지 않습니다.
- **하늘색(`--brand` #00afec) 위 흰 글씨는 대비 2.5:1** 로 기준 미달입니다. 단체 색이라
  사람이 정할 일이라 두었습니다. 고치려면 흰 글씨가 얹히는 곳만 `--brand-deep`(4.1:1).

---

## 붙이기 전 점검표

- [ ] `python tools/check.py --net` (인증서는 못 잡습니다)
- [ ] `build-activity.py` · `build-campaign-together.py` · `build-static.py` 를 `--check` 로
      돌려 생성물이 원본과 맞는지
- [ ] `dist/` 에 `확인 필요` 가 남았는지
- [ ] 세 캠페인 주소가 브라우저에서 경고 없이 열리는지
- [ ] 헤더·푸터는 반복 섹션에 따로 붙습니다(화면마다 복사되지 않습니다)

---

## 미리보기

```
python tools/preview.py          →  http://127.0.0.1:8777/
python tools/preview.py 8899     →  포트를 바꿔서
```

이 저장소의 `.html` 은 통째로 된 문서가 아니라 **코드 위젯에 붙일 조각**입니다.
`<head>` 가 없어 그냥 열면 한글이 깨집니다. 이 서버가 charset 을 씌워 줍니다.

검토 사이트는 `main` 에 push 하면 자동으로 다시 구워집니다. 손으로 굽는 법:

```
python tools/build-static.py --base action-home-preview
# dist/ 를 action-home-preview 저장소에 밀어 넣는다 (.git · README.md 는 남긴다)
```

---

## 문서

| 파일 | 무엇 |
|---|---|
| `AGENTS.md` · `CLAUDE.md` | 작업 규칙. 둘은 내용이 같습니다 |
| `WORKING_RULES.md` | 무엇을 만들까 — 프로젝트 규칙 |
| `CONTENT.md` | `window.AH_DATA` 형식, 게시판 제목 규칙 |
| `DESIGN.md` | 색·글꼴·모양 한 벌 |
| `CAMPAIGNERS_CODE_SPLIT.md` | 원본과 생성물, 붙이는 순서 |
| `CAMPAIGNUS_IMPLEMENTATION.md` | 캠페이너스가 기본으로 해주는 것과 못 해주는 것 (2026-08-31 조사) |
| `DEPLOYMENT.md` | 안 하기로 한 Netlify 설계 — **기록입니다** |
| `PRIVACY*.md` | 개인정보처리방침 정본 셋 |

---

## 기록

> 여기에 덧붙입니다. 최신이 위. **무엇을 했는지보다 왜 그렇게 정했는지**를 적습니다.

## [2026-09-11] 옛 작업 일지를 걷어냈다

README 가 732줄까지 불어나 있었다. 지나간 내용을 지우는 대신 "이건 지나간 얘기입니다"
딱지를 붙여 둬서 **맞는 내용과 틀린 내용이 한 파일에 섞여** 있었고, 정작 일지는
9월 3일에서 멈춘 채 `HANDOFF_2026-09-08.md` 가 새 입구 노릇을 하고 있었다.

- 걷어낸 것 — 8월 27일~9월 3일 날짜별 작업 일지 전체, 이미 해결된 장애(후원 링크 404,
  `vote.action.or.kr` 404), 답을 받은 「확인 부탁」, 옛 mrmweb 후원 구조, 상담소가
  이 저장소에 있던 시절의 안내.
- 접은 것 — `HANDOFF_2026-09-08.md` 와 `HANDOFF_CAMPAIGN_2026-09-02.md` 를 이 파일로
  들여오고 지웠다. 캠페인 제목 규칙은 `CONTENT.md` 에 이미 있어 옮기지 않았다.
- 남긴 것 — 붙이는 구조, 되묻지 말 것, 알려진 함정, 답을 기다리는 물음.
  **고쳐야 할 이유가 아직 살아 있는 것만** 남겼다.
- 걷어내기 전 모습은 커밋 `d595d8a` 에 있다.

**왜** — 기록을 안 해서가 아니라 **적을 곳이 무거워질 때마다 새 파일을 만들어서**
마크다운이 열넷까지 늘었다. 새로 만들지 말고 걷어내자는 것이 `AGENTS.md` 2번 규칙이다.
