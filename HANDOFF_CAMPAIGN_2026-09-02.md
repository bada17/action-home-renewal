# 캠페인 홈페이지 작업 인계 — 2026-09-02

## 저장소와 현재 상태

- 저장소: `C:\Users\dbqke\OneDrive\문서\ChatGPT\밑빠진독상 페이지\action-home-renewal`
- 현재 변경사항은 **커밋하거나 푸시하지 않았다.**
- 작업 중 Claude가 같은 저장소를 함께 고쳤으므로, 기존 변경을 되돌리거나 파일 전체를 덮어쓰지 않는다.
- 특히 `dok-history.html`은 Claude가 별도로 수정을 진행한 파일이다.

## 역할 구분과 범위

- Claude 담당: 개별 외부 캠페인 사이트
  - `밑빠진 독상`
  - `10만원 예산편성`
  - `참여예산 상담소`
- Codex 담당: 시민행동 홈페이지에서 위 외부 캠페인으로 연결되는 구조
  - 홈페이지 캠페인 영역
  - `함께하기` 캠페인 모음
  - 진행 중·종료 캠페인 게시판 RSS 연결
  - 공통 상단의 캠페인 메뉴 연결
- Claude가 맡은 외부 사이트 저장소는 사용자의 별도 요청이 없으면 건드리지 않는다.

## 확정한 캠페인 구조

### 홈페이지

- 원본 파일은 `index.html`이다.
- 진행 중 캠페인 게시판의 글을 읽어 홈페이지 캠페인 영역에 표시한다.
- 캠페이너스 홈페이지 코드 위젯에는 `index.html`의 페이지 코드 전체를 붙인다.

### 함께하기

- 수정 원본은 `issue.html` 한 곳이다.
- 캠페이너스에는 아래 조각을 연달아 붙인다.
  - `campaign-together-layout.html`
  - `campaign-together-scripts.html`
  - 공통 하단 반복 섹션
- 두 코드 조각 사이에 캠페이너스 기본 게시판 위젯을 넣지 않는다.
- 분리 파일은 `tools/build-campaign-together.py`로 생성한다. 생성 파일을 직접 수정하지 않는다.

### 진행 중 캠페인 게시판과 종료 캠페인 게시판

- 두 게시판은 홈페이지와 `함께하기`에 표시할 자료를 입력하는 곳이다.
- 상단 메뉴에는 두 게시판을 직접 노출하지 않는다.
- 게시글 하나를 캠페인 하나로 사용한다.
- 진행 중 캠페인을 끝낼 때는 종료 게시판으로 옮긴다.
- 게시글 제목과 대표 이미지를 홈페이지와 `함께하기` 카드가 함께 사용한다.

## 캠페인 게시글 제목 작성 규칙

외부 캠페인:

```text
[링크:https://외부주소/] 캠페인 제목 — 한 줄 설명
```

홈페이지 내부 캠페인:

```text
[링크:/내부주소] 캠페인 제목 — 한 줄 설명
```

- `[링크:...]` 표시는 카드 화면에서 숨긴다.
- 긴 대시 `—` 앞은 제목, 뒤는 한 줄 설명이다.
- 안전한 `https://` 외부 주소만 받는다.
- 외부 캠페인은 **새 탭**으로 열고 제목 옆에 `↗`를 표시한다.
- 새 탭 링크에는 `target="_blank"`와 `rel="noopener noreferrer"`를 적용했다.
- `/`로 시작하는 내부 주소는 같은 탭으로 연다.
- 링크 표시가 없거나 주소가 안전하지 않으면 원래 게시글을 같은 탭으로 연다.
- 외부 목적지를 써도 진행 중·종료 판별은 원래 게시글 주소로 한다.

## 이번 대화에서 반영한 코드

- `index.html`
  - 외부 캠페인 카드만 새 탭으로 열도록 수정
  - 외부 카드 제목에 `↗` 표시
  - 내부 링크와 원래 게시글은 같은 탭 유지
- `issue.html`
  - 홈페이지와 같은 새 탭·내부 링크 규칙 적용
- `campaign-together-layout.html`
  - `issue.html`에서 다시 생성
- `campaign-together-scripts.html`
  - `issue.html`에서 다시 생성
- 운영 기록 갱신
  - `CONTENT.md`
  - `README.md`
  - `WORKING_RULES.md`
  - `CAMPAIGNERS_CODE_SPLIT.md`

확인 결과:

- 함께하기 생성기 재실행 및 `--check` 통과
- `index.html`, `issue.html`, `campaign-together-scripts.html`의 인라인 스크립트 구문 검사 통과
- 외부 주소, 내부 주소, 링크 없음, `javascript:` 주소, `//` 주소의 다섯 경우 검사 통과
- `git diff --check` 통과

## 아직 바꾸지 않은 것

### 공통 상단의 개별 캠페인 메뉴

- `tools/parts/header.html`의 개별 캠페인 세 항목은 아직 임시 캠페이너스 내부 주소를 사용한다.
- Claude가 외부 사이트의 **최종 정상 작동 주소**를 넘기면 데스크톱 메뉴와 모바일 메뉴를 함께 바꾼다.
- 외부 주소로 바꿀 때 새 탭, `noopener noreferrer`, `↗` 또는 새 창 안내도 함께 적용한다.
- 현재 알려진 방향은 아래와 같지만, 정상 접속 확인 전에는 공통 상단에 적용하지 않는다.
  - `10만원 예산편성`: `https://vote.action.or.kr/`
  - `밑빠진 독상`: `https://dok.action.or.kr/` 예정
  - `참여예산 상담소`: `https://pb.action.or.kr/` 예정
- 마지막 확인 당시 `dok.action.or.kr`과 `pb.action.or.kr`은 아직 정상 접속할 수 없었다.

### HTML에 남은 임시 카드

- RSS 게시글이 없거나 연결이 실패하면 화면이 비지 않도록 임시 카드가 남는다.
- 이 임시 카드는 아직 캠페이너스 내부 주소로 연결된다.
- 진행 중 캠페인 게시판에 실제 글을 올리고 제목에 외부 링크 규칙을 넣으면 RSS 카드가 임시 카드를 대체하며 새 탭으로 열린다.

## 시민행동 로고가 이전 홈페이지로 가는 이유

- 공통 상단의 시민행동 로고는 홈페이지 루트 `/`로 연결된다.
- 새 홈페이지를 미리보기 주소에서 보고 있을 때 로고를 누르면 현재 공개 중인 이전 홈페이지가 나오는 것이 정상이다.
- 새 `index.html` 코드를 실제 홈페이지에 붙이고 홈페이지 자체를 게시·교체하면 로고를 눌렀을 때 새 홈페이지가 열린다.
- `함께하기`만 게시하고 홈페이지를 교체하지 않으면 로고는 계속 이전 홈페이지로 간다.

## 다음 대화에서 할 일

### 캠페이너스 코드 갱신

- 이번 수정 뒤의 새 `index.html`을 홈페이지 코드 위젯에 다시 붙인다.
- 새 `campaign-together-layout.html`과 `campaign-together-scripts.html`을 `함께하기`의 두 코드 위젯에 다시 붙인다.
- 저장만 한 것인지 실제 게시까지 한 것인지 확인한다.

### 외부 캠페인 주소 연결

- Claude에게 세 외부 캠페인의 최종 주소와 정상 접속 여부를 받는다.
- 진행 중 캠페인 게시판 글 제목의 `[링크:...]` 주소를 최종 주소로 바꾼다.
- 공통 상단의 개별 캠페인 메뉴도 같은 최종 주소로 바꾸고 새 탭으로 연다.

### 실제 화면 확인

- 홈페이지와 `함께하기`에서 카드 제목에 `[링크:...]`가 보이지 않는지 확인한다.
- 외부 캠페인 카드가 새 탭으로 열리고 `↗`가 보이는지 확인한다.
- 내부 링크와 시민행동 홈페이지 메뉴는 같은 탭으로 열리는지 확인한다.
- 데스크톱과 모바일에서 카드, 상단 메뉴, 새 탭 동작을 확인한다.
- 시민행동 로고가 새 홈페이지로 가는지는 홈페이지 자체를 실제 게시한 뒤 확인한다.

## 다음 Codex에게 먼저 전할 말

> 외부 캠페인 사이트 자체는 Claude 담당이다. 이 저장소에서는 홈페이지·함께하기·공통 상단 연결만 다룬다. 현재 외부 캠페인 카드는 게시글 제목의 `[링크:https://...]`를 읽어 새 탭으로 열고 `↗`를 표시하도록 구현되어 있다. 생성 파일을 직접 고치지 말고 `issue.html`과 생성기를 사용한다. 공통 상단의 세 개별 캠페인 링크는 최종 외부 주소가 정상 작동하기 전이라 아직 내부 임시 주소다. 변경사항은 커밋·푸시되지 않았다.
