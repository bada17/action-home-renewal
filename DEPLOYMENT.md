# 배포 — 지금은 손 붙여넣기, Netlify 는 뒤로 미룸

**2026-09-01 사용자 결정.** GPT 가 보낸 자동배포 사양에 대한 답입니다.

> 사용자: "내부에서 된다면 그동안 Netlify 자동배포 필요없잖아."

**버리는 것이 아니라 순서를 뒤로 옮깁니다.** 아래에 왜 미뤘는지, 언제 다시 꺼낼지,
다시 꺼낼 때 어떻게 지을지를 적어 둡니다. 검토한 것을 버리지 않으려는 문서입니다.

---

## 1. 두 가지를 갈라서 보아야 합니다

자동배포 사양은 두 문제를 한 덩어리로 묶고 있었습니다. 실제로는 따로입니다.

| | 무엇이 바뀔 때 | 없으면 누가 고생하나 |
|---|---|---|
| **내용** — 캠페인·글·사진 | 캠페인을 새로 열 때, 글을 올릴 때 | 매번 Claude 를 거쳐야 함 |
| **코드** — 디자인·기능·버그 | 화면을 고칠 때 | GPT 가 위젯 아홉 곳에 다시 붙여넣어야 함 |

**Netlify 자동배포는 아래쪽(코드)만 풉니다.** 캠페인을 게시판에서 받으면 위쪽은
코드 배포 없이 해결되고, 일상 운영에서는 Netlify 가 할 일이 없습니다.

## 2. 왜 지금은 안 하나 — 세 가지

1. **아직 캠페이너스에 붙이지 않았습니다.** 지금은 `tools/preview.py` 로 보고 있어
   붙여넣기 부담이 0 입니다. 아프지 않은 곳을 미리 고치는 셈입니다.
2. **캠페이너스가 코드 위젯에서 외부 `<script>` 를 실행하는지 아직 확인되지 않았습니다.**
   (README 에도 미확인으로 남아 있습니다.) 실행되지 않으면 `netlify.toml`·로더·이 문서가
   통째로 버려집니다. **이것부터 확인해야 합니다 — GPT 몫입니다.**
3. **이 저장소의 원칙과 어긋납니다** — "캠페이너스에 이미 있는 기능은 새로 만들지 않는다.
   새로 지으면 우리가 평생 고쳐야 한다"(`CONTENT.md`). Netlify + 자체 로더는 외부 서비스
   하나와 우리가 평생 돌볼 코드 하나를 늘립니다. 로더가 죽으면 화면이 안 뜹니다
   (지금은 코드가 페이지에 박혀 있어 최소한 뜹니다).

## 3. 그래서 순서

1. **캠페인 게시판 두 개**(진행 중 · 종료)를 만들고 화면에 연결한다 ← **지금 할 일**
2. 캠페이너스 숨김 페이지에 붙여서 실제로 운영해 본다
3. **코드 붙여넣기가 실제로 아프면** 그때 Netlify 를 꺼낸다

### 다시 꺼낼 신호

- 헤더·푸터를 고칠 때마다 아홉 화면을 손으로 붙여넣는 일이 **반복해서** 생긴다
- 그리고 2번에서 **외부 `<script>` 가 실행된다는 것이 확인되었다**

둘 다여야 합니다. 두 번째가 아니면 지어도 못 씁니다.

---

## 4. 다시 꺼낼 때 — 이렇게 지으면 됩니다

검토는 끝나 있습니다. 아래는 2026-09-01 에 저장소를 뜯어보고 정리한 설계입니다.

### 핵심 — 조각을 셋으로 쪼갠다

이 저장소의 `.html` 은 **`<style>` + 마크업 + `<script>` 가 한 파일에 섞인 조각**입니다
(홈은 style 2 · script 3). 그대로 `innerHTML` 로 넣으면 **스크립트가 실행되지 않습니다.**
그렇다고 `eval` · `document.write` 는 쓰지 않기로 했습니다.

→ 새 스크립트 `tools/build-embed.py` 가 조각 하나를 셋으로 나눕니다.

    dist/embed/home.<해시>.css     <style> 을 모아서
    dist/embed/home.<해시>.html    마크업만
    dist/embed/home.<해시>.js      <script> 를 모아서
    dist/embed/manifest.json       페이지키 → 위 세 주소
    dist/embed/ah-loader.js        위젯에 넣을 로더

로더는 `<link href>` 와 `<script src>` 로 **외부 파일을 붙이기만** 합니다.
인라인 스크립트가 하나도 없어서 CSP 를 통과할 가능성이 가장 높습니다.

### 캠페이너스 위젯에 넣을 것

    <div id="ah-root" data-ah-page="home"></div>
    <script src="https://<네틀리파이주소>/embed/ah-loader.js"></script>

### 미리 찾아 둔 함정 셋

- **`DOMContentLoaded`** — 헤더 스크립트가 이것으로 헤더 높이를 잽니다
  (`tools/parts/header.html` 의 `measureBar`). 로더가 붙일 때는 이미 지난 뒤라
  **영영 실행되지 않습니다.** `build-embed.py` 가 이 호출만 `AH.ready(...)` 로 바꾸고,
  로더가 "이미 로드됐으면 즉시 실행"을 제공해야 합니다.
- **스티비** — 홈이 `resource.stibee.com` 의 CSS·JS 를 부릅니다. CSS 묶음에 넣지 말고
  진짜 `<link>` · `<script src>` 로 살려 둘 것. 그 도메인이 막히는지도 확인 대상입니다.
- **CORS** — 캠페이너스에서 Netlify 파일을 `fetch` 하므로 `Access-Control-Allow-Origin: *`
  가 필요합니다. `netlify.toml` 헤더에 넣습니다.

### 캐시

| 무엇 | 정책 | 왜 |
|---|---|---|
| 해시 붙은 css·html·js | `max-age=31536000, immutable` | 주소가 바뀌면 새 파일이라 캐시가 문제되지 않음 |
| `manifest.json` · `ah-loader.js` | `no-cache` (항상 재검증) | 로더 주소는 위젯에 박혀 못 바꾸므로, 이 둘이 늘 최신이어야 새 해시를 물어옴 |

### 실패 처리

`#ah-root` 는 비어서 시작하고, 로더는 실패해도 **아무것도 지우지 않습니다.**
콘솔 경고만 남기고 캠페이너스 페이지는 멀쩡히 남습니다. 중복 실행은 `window` 에 둔
표시 하나로 막습니다.

### 빌드

`tools/build-static.py` 는 표준 라이브러리만 쓰므로 Netlify 에서 그대로 돕니다.
밑빠진 독상 데이터는 옆 저장소에서 복사하는데 Netlify 에는 그 폴더가 없습니다 —
**경고만 찍고 건너뜁니다(죽지 않습니다).** 그 화면은 원격에서 `awards.json` 을 받게 되고,
dokseong 에 CORS 가 열려 있어 동작합니다.

### 그때도 지킬 것

- 검증은 **한 페이지만** 먼저 (홈 또는 캠페인 목록)
- 로컬 미리보기 → Netlify 주소 → **캠페이너스 숨김 초안**(⚠️ `/69` 아님, 새 페이지) 순서
- 공개 페이지와 `/69` 에는 아무 변경도 하지 않음
- 저장소에 Netlify 토큰·캠페이너스 로그인 정보를 넣지 않음
  (이 저장소는 2026-09-01 부터 **공개**입니다)

---

## 5. 지금의 배포 방법

`tools/build-parts.py` · `tools/build-activity.py` 로 화면을 찍은 뒤,
**GPT 가 캠페이너스 코드 위젯에 붙여넣습니다.** 화면마다 한 번씩입니다.
게시는 사용자 승인 없이 하지 않습니다(`WORKING_RULES.md` 4번).
