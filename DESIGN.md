# 디자인 한 벌 (2026-08-26)

다섯 화면이 "같은 단체가 만든 것"으로 보여야 합니다. 그러려면 아래 값이
**모든 파일에서 글자 하나까지 같아야** 합니다. 파일마다 다른 값을 쓰면
색이 미묘하게 어긋나고, 그게 "중구난방"으로 보이는 원인입니다.

각 파일은 캠페이너스 코드 위젯이라 스타일시트를 공유할 수 없습니다.
그래서 **복사해서 붙이되, 값은 절대 바꾸지 않습니다.** 값을 바꿔야 하면
이 문서를 먼저 고치고 다섯 파일에 똑같이 반영하세요.

## 색

```
--brand:      #00afec   시민행동 하늘색. 현재 홈이 쓰는 색
--deep:       #0079a6   흰 바탕 위 글자·링크에 쓰는 진한 쪽 (대비 4.5:1 넘김)
--tint:       #e2f5fd   옅은 하늘. 마감 표시 배경
--band:       #eef7fb   띠·섹션 배경
--navy:       #062330   히어로 바탕
--ink:        #0b1e28   본문 글자
--soft:       #4a6473   보조 글자
--faint:      #7d95a3   날짜·메타
--line:       #d5e4eb   선
--paper:      #f1f6f8   옅은 바탕
--flag:       #9a5c06   '확인 필요' 딱지 글자
--flag-bg:    #fff4e1   딱지 배경
--flag-line:  #ecd3a2   딱지 테두리
```

**주황(--flag)은 딱지에만 씁니다.** 레퍼런스(아름다운재단)의 주황을 본문에
쓰면 시민행동이 그 단체처럼 보입니다.

## 글꼴

- 제목: `GmarketSans` (700) — 랜딩과 같은 것
- 본문: `Pretendard Variable`, `Pretendard`, `Noto Sans KR`
- 자간 `letter-spacing:-.022em` (제목), 줄간 `1.65`(본문) / `1.35`(제목)
- `word-break:keep-all` — 한글이 단어 중간에서 안 끊기게

## 모양 규칙 — 이게 통일감의 대부분입니다

| 무엇 | 모서리 | 왜 |
|---|---|---|
| 카드 · 블록 · 히어로 · 표 | **각짐 (0)** | 내용이 담기는 곳 |
| 태그 · 뱃지 · 버튼 · 딱지 | **알약 (999px)** | 누르거나 표시하는 것 |

둥근 카드와 각진 카드를 섞지 않습니다. 예전 코드에는 16px·13px·0 이
섞여 있었고 그게 화면마다 다른 느낌을 준 원인입니다.

## 영어 라벨을 쓰지 않습니다 (2026-08-27)

한글 제목 위에 `NOW` · `RESULT` · `MAP` 같은 영어 눈썹줄(`.eyebrow`)을 달던 것을
**전부 걷어냈습니다.** 사용자 지시입니다.

바로 밑에 같은 말이 한글로 있어서 뜻을 더하지 못했고, 읽는 사람에게는
장식일 뿐이었습니다. 스타일 규칙(`.eyebrow` · `.dok-section-tag`)도 같이 지웠습니다 —
남겨 두면 다음에 누군가 되살립니다.

로고의 `ACTION TOGether` 는 단체의 영문 이름(로고 한 벌)이라 남겨 두었습니다.
이것도 뺄지는 확인이 필요합니다.

## 캠페인 카드 — 액자 (2026-08-27)

사용자 지시: **"글자부분과 사진부분을 철저하게 분리해. 사진에서 통일감을 줄 수 없어.
그러니 액자 같은 틀에서 통일감을 줘야해."**

예전에는 사진을 40% 로 깔고 어둠을 덮은 뒤 그 위에 흰 글씨를 얹었습니다.
사진마다 밝기·구도가 달라 같은 어둠을 덮어도 카드가 제각각으로 보였습니다.

지금 규칙 넷 — 홈 첫 화면(`.slide`)과 이슈 카드(`.card`)가 같이 지킵니다.

1. **위는 액자, 아래는 흰 바탕 글자 칸.** 둘이 겹치지 않습니다.
2. **액자 비율은 4:3 고정**, 테두리·매트 색은 카드마다 같습니다.
   통일감은 사진이 아니라 액자가 냅니다.
3. **`object-fit:contain`.** `cover` 는 액자에 맞추려고 사진을 잘라 냅니다.
   우리 이슈 사진은 포스터·카드뉴스라 잘리면 제목부터 날아갑니다
   (실제로 '한강버스 주요 연혁'의 첫 글자가 잘렸습니다).
   **홈 첫 화면은 여기에 한 겹 더 있습니다** — 사진을 흐리게 확대한 것을 뒤에 깔아
   액자를 채웁니다(`.fill` + `.real`). 색 매트만 깔면 사진 비율에 따라 여백 크기가
   달라져 장마다 "칸이 다르다"고 보입니다.
4. **액자 위에는 아무것도 얹지 않습니다.** D-day 도 '진행 중' 뱃지도 글자 칸에 있습니다.
   모서리에 얹으면 포스터 제목을 가립니다.
5. **액자 비율은 못박고, 글자 칸 높이는 정하지 않습니다.**
   홈 첫 화면의 다섯 장은 `position:absolute` 로 겹치지 않고 **격자의 같은 칸**에
   쌓습니다(`grid-area:1/1`). absolute 로 겹치면 칸 높이가 내용과 무관해져서
   글이 긴 장은 글자가 잘렸습니다. 같은 칸에 쌓으면 높이가 제일 긴 장에 맞춰집니다.

사진이 없는 칸은 **점선 빈 상자로 두지 않습니다.** 색 타일(`--tile`)에 큰 글자를
깔아 사진 있는 카드와 무게를 맞춥니다. 점선을 쓰면 그 카드만 미완성으로 보입니다.

소식 카드(`.pcard`)가 원래 이 모양이었고, 이제 셋이 같은 한 벌입니다.

## 화면 맨 위 — 색 띠를 두르지 않습니다 (2026-08-27)

사용자 지적: **활동 페이지에 들어가면 띠가 겹겹이 쌓여 마음에 안 든다.**

예전에는 짙은 남색 띠(히어로) 밑에 하늘색 띠(숫자 셋)가 또 있었습니다.
색면 둘이 위아래로 붙어 제목이 그 안에 갇히고, 본문까지 내려가는 데 화면 하나를 다 썼습니다.

지금은 **흰 바탕 한 덩이**입니다.

```
빵부스러기 → (라벨) → 큰 제목 → 한 줄 → (숫자 셋)
──────────────────────────────────── 2px 잉크 선
본문
```

- 색면을 쓰지 않습니다. 마감은 **밑변의 2px 잉크 선 하나**입니다.
- 숫자 셋은 따로 띠를 두르지 않고 이 덩이의 마지막 줄로 들어갑니다.
  **왼쪽 정렬**입니다 — 가운데로 놓으면 위의 제목과 축이 어긋나 또 따로 노는 칸이 됩니다.
- 활동 넷 · 이슈 · 자료실이 **같은 틀**을 씁니다.
  (밑빠진 독상은 손대지 않기로 확정돼 있어 제외입니다.)

## 페이지마다 다르게 디자인하지 않습니다

**틀은 하나, 다른 것은 내용이 담당합니다.**

형제 페이지끼리 틀이 다르면 다른 사이트로 읽히고, 무엇보다 고칠 때 네 번 고쳐야 해서
반드시 어긋납니다(색이 `#0087b8` 과 `#0079a6` 으로 갈렸던 게 그래서였습니다).

구별은 **활동마다 하나씩 있는 고유한 칸**이 이미 하고 있습니다 —
지역 태그 / 물음과 답 / 한 해 달력 / 들어오는 문. 여기에 틀까지 다르게 하면
구별이 아니라 산만함이 됩니다.

## 되풀이되는 부품

- `.sec-hd` — 두 줄 큰 제목 + 오른쪽 '전체보기 →'
- `.tbd` — 확인 필요 딱지. 공개 전에 화면에 하나도 없어야 함
- `.crumb` — 빵부스러기. 홈 › 활동 › (현재)
- 맨 위(`.top`) — 흰 바탕, 밑변 2px 잉크 선. 위 '화면 맨 위' 절을 볼 것

## 움직임

- 스크롤 리빌: 숨기는 상태를 **스크립트가 붙임**. 막히면 그냥 다 보임
- 첫 화면 자동 순환: 6초. 마우스·키보드가 올라오면 멈춤
- `prefers-reduced-motion: reduce` 면 전부 끔

## 공통 헤더 (2026-08-27 신설)

여섯 화면이 **하나의 헤더 부품**을 나눠 씁니다.

- 원본은 `tools/parts/header.html` **한 곳뿐**입니다. 여기만 고치세요.
- `python tools/build-parts.py` 가 각 .html 의 `<!-- PART:header -->` 사이에 복사해 넣습니다.
  (활동 페이지는 `build-activity.py` 가 끝에 알아서 불러 줍니다.)
- `--check` 를 붙이면 고치지 않고 어긋난 곳만 알려 줍니다.

지켜야 할 것 셋:

1. **헤더는 페이지 뿌리 밖에 둡니다.** 뿌리(`#act` 등)에 `overflow:hidden` 이 걸려 있어서,
   그 안에 넣으면 `position:sticky` 가 안 붙습니다.
2. **범위는 `#ah-nav`** 입니다. 화면마다 뿌리 이름이 달라 뿌리에 기대면 CSS 가 안 먹습니다.
3. **전체 폭은 여백으로만** 만듭니다 — `margin-left:calc(50% - 50vw + var(--ah-sb,0px)/2)`.
   `left:50%` 는 쓰면 안 됩니다. sticky 에서 `left` 는 위치가 아니라 **가로로 붙는 조건**입니다.
   `--ah-sb` 는 세로 스크롤바 폭이고 헤더 스크립트가 재서 넣습니다. 이걸 빼면 모든 화면이
   스크롤바 폭만큼 옆으로 밀려 가로 스크롤바가 생깁니다.

### 지금 보고 있는 곳 표시

헤더의 `data-here` 값과 같은 `data-nav` 를 가진 링크에 `.is-here` 가 붙습니다.
**색과 밑줄 두 가지로** 표시합니다 — 색만 쓰면 색각 이상이 있는 분에게 안 보입니다.

### 후원하기 버튼

빛줄기(4초에 한 번) + 옅은 테 숨쉬기 + 하트. **계속 번쩍이지 않습니다** —
그러면 눈이 아프고 광고처럼 보입니다. `prefers-reduced-motion` 이면 전부 꺼집니다.

## 활동 페이지 — 활동마다 다른 칸 (2026-08-27)

셋이 한 틀에서 나와 글자만 다르고 그림이 똑같다는 문제를 풀었습니다.
무늬로 구별하지 않고, **활동마다 답하는 질문이 다르다**는 데서 모양을 끌어냈습니다.

| 활동 | 답하는 질문 | 칸 |
|---|---|---|
| 지자체 감시 | 어디가 어떤가 | 들여다본 지역 이름을 늘어놓기 |
| 권력감시 | 누가 답을 안 하나 | 물음과 답변 여부를 나란히 |
| 예산감시 | 언제 무엇이 정해지나 | 한 해 흐름 위에 오늘 찍기 |
| 시민참여 | 시민이 어디로 들어오나 | 들어오는 문과 열림 여부 |

시민참여만 질문의 방향이 반대입니다. 나머지 넷은 "우리가 무엇을 보는가"를 말하지만,
이 활동은 **"시민이 무엇을 할 수 있는가"**를 말해야 합니다. 그래서 감시 대상이 아니라
들어오는 문을 늘어놓고, 줄마다 누를 수 있게 했습니다.

예산 달력의 칸 너비는 **날짜 비율 그대로**입니다. 균등하게 나누면 '9월 2일'이
한 해의 3분의 1 지점인 것처럼 잘못 읽힙니다.

## 파일

| 파일 | 무엇 | 범위 |
|---|---|---|
| `index.html` | 홈 | `#action-home-v2` |
| `activity-local.html` | 활동 > 지자체 감시 | `#act` |
| `activity-power.html` | 활동 > 권력감시 | `#act` |
| `activity-budget.html` | 활동 > 예산감시 | `#act` |
| `dok-history.html` | 활동 > 밑빠진 독상 | `#dok` |
| `issue.html` | 이슈 목록 | `#iss` |
| `pb.html` | 참여예산 상담소 | `#pb` |
| `activity-civic.html` | 활동 > 시민참여 | `#act` |
| `library.html` | 자료실 (`build-library.py` 가 찍어냄) | `#lib` |
| `tools/parts/header.html` | **공통 헤더** (여섯 화면이 나눠 씀) | `#ah-nav` |

`#dok` 과 참여예산 상담소(`#pb`)는 성격이 달라 구성은 다르지만,
**색·글꼴·딱지·빵부스러기·히어로는 위 값을 그대로 씁니다.**
